import { cn } from '../../lib/cn';
export const statusLabels: Record<string, string> = { new: 'Prospek baru', identifying: 'Identifikasi', offered: 'Ditawarkan', objection: 'Keberatan', followup: 'Follow-up', closing: 'Closing', closed_won: 'Deal menang', closed_lost: 'Batal', nurture: 'Nurture' };
export function Badge({ value, className }: { value: string; className?: string }) { return <span className={cn('inline-flex items-center rounded-full border px-2.5 py-1 text-[10px] font-bold uppercase tracking-wide', value === 'closed_won' ? 'border-emerald-600 bg-emerald-600 text-white' : value === 'closed_lost' ? 'border-zinc-200 bg-zinc-200 text-zinc-500 line-through' : ['objection','followup','closing'].includes(value) ? 'border-zinc-800 bg-white text-zinc-900' : 'border-zinc-200 bg-zinc-100 text-zinc-600', className)}>{statusLabels[value] ?? value}</span>; }
